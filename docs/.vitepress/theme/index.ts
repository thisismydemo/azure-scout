import DefaultTheme from 'vitepress/theme'
import './custom.css'

// The default theme, plus the overrides in custom.css. Nothing here changes behaviour —
// see custom.css for what is restyled and why.
export default DefaultTheme
