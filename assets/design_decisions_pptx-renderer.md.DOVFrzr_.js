import{_ as t,o,c as r,a2 as n}from"./chunks/framework.il0noOp3.js";const u=JSON.parse(`{"title":"Decision record — PPTX executive-deck renderer","description":"Decision record — executive-deck (PPTX) renderer for the Report layer's Tier 3 output. Resolves the open item flagged in master-plan.md §9a.","frontmatter":{"description":"Decision record — executive-deck (PPTX) renderer for the Report layer's Tier 3 output. Resolves the open item flagged in master-plan.md §9a."},"headers":[],"relativePath":"design/decisions/pptx-renderer.md","filePath":"design/decisions/pptx-renderer.md"}`),d={name:"design/decisions/pptx-renderer.md"};function a(i,e,s,c,l,p){return o(),r("div",null,[...e[0]||(e[0]=[n('<h1 id="decision-record-—-pptx-executive-deck-renderer" tabindex="-1">Decision record — PPTX executive-deck renderer <a class="header-anchor" href="#decision-record-—-pptx-executive-deck-renderer" aria-label="Permalink to &quot;Decision record — PPTX executive-deck renderer&quot;">​</a></h1><blockquote><p><strong>Status:</strong> Accepted 2026-07-22. The repo owner deferred the library choice to this record and stated the acceptance criterion directly: <strong>the output must be professional-looking PowerPoint decks.</strong> That requirement is met by the template-driven approach below — visual quality comes from the designer-authored <code>deck.pptx.template</code> (slide masters, brand colors, typography, authored once in PowerPoint Desktop), not from the fill library; the OpenXML SDK is only the mechanism that populates it. If the first rendered decks don&#39;t meet the bar visually, iterate on the template asset, not the renderer.</p><p><strong>Date:</strong> 2026-07-22 · <strong>Author:</strong> Claude Code (engineer agent) · <strong>Unblocks:</strong> AB#5044 (Tiered renderer engine)</p></blockquote><h2 id="_1-decision" tabindex="-1">1. Decision <a class="header-anchor" href="#_1-decision" aria-label="Permalink to &quot;1. Decision&quot;">​</a></h2><p><strong>Recommend: OpenXML SDK (<code>DocumentFormat.OpenXml</code>), not <code>python-pptx</code>, not COM automation.</strong></p><p>Build the executive deck by opening a pre-authored <code>deck.pptx.template</code> (title + content slide layouts, styled once in PowerPoint, per the layout already named in <code>enhancement-spec.md</code> §1.1) and populating it programmatically via the OpenXML SDK — cloning the repeating slide (area scores, gap list, manual review) and replacing text runs, rather than hand-building the OPC part tree from nothing.</p><h2 id="_2-why-this-repo-s-actual-constraints" tabindex="-1">2. Why (this repo&#39;s actual constraints) <a class="header-anchor" href="#_2-why-this-repo-s-actual-constraints" aria-label="Permalink to &quot;2. Why (this repo&#39;s actual constraints)&quot;">​</a></h2><ul><li><strong>CI is <code>ubuntu-latest</code></strong> (<code>.ado/azure-pipelines.yml:4</code>) — this alone eliminates COM automation outright; it needs PowerPoint installed and is Windows-only. Not a real option for an unattended pipeline that has to run on a Linux Microsoft-hosted agent.</li><li><strong>python-pptx adds a second runtime the pipeline doesn&#39;t otherwise need.</strong> Every other renderer (<code>Export-Html.ps1</code>, <code>Export-Excel.ps1</code> via <code>ImportExcel</code>/EPPlus, <code>Export-PowerBi.ps1</code>) is pure PowerShell/.NET. The current prototype (<code>src/report/renderers/Export-Pptx.ps1:16-18</code>) shells out to <code>python</code> and only checks that the <code>python</code> binary exists — it does not check that <code>python-pptx</code> is installed, and <code>.ado/azure-pipelines.yml</code> has no <code>pip install</code> step. Today, a pipeline run with <code>-OutputFormat Pptx</code> either silently skips the deck (if <code>python</code> itself is missing) or crashes on an unguarded <code>ImportError</code> inside <code>build_deck.py</code> (if <code>python</code> exists but the package doesn&#39;t) — this is a live gap, not a hypothetical one.</li><li><strong>The tool&#39;s stated identity is &quot;PowerShell module,&quot; cross-platform (Windows/Linux/macOS per <code>README.md</code> frontmatter), no licensing fees, no extra runtimes.</strong> Every other collect/assess/report component is PowerShell + .NET (Az modules, <code>ImportExcel</code>, <code>powershell-yaml</code>). Adding Python for exactly one of five output tiers means every environment that wants the full <code>-OutputFormat All</code> matrix now needs two interpreters provisioned and kept in sync, doubling the dependency surface for one slide deck.</li><li><strong>OpenXML SDK is pure .NET, ships as a NuGet package, and loads with <code>Add-Type</code> inside PowerShell 7 the same way <code>ImportExcel</code> already loads EPPlus.</strong> No separate interpreter, no <code>pip</code>, no version drift between a Python venv and the PowerShell host, works identically wherever <code>pwsh</code> already runs (Windows, the Linux CI agent, or a contributor&#39;s Mac).</li><li><strong>The &quot;more code&quot; cost is real but bounded.</strong> The deck only needs four slide shapes (title, framework scores, area scores, prioritized gaps, manual-review worklist) — all bullet-list content, no charts or images. Templating against a pre-built <code>.pptx</code> (clone one populated slide N times, replace text runs) avoids hand-assembling the full Open Packaging Conventions part tree, which is where OpenXML SDK code volume usually balloons. This keeps the SDK route&#39;s added code proportional to the content, not to the file format&#39;s complexity.</li><li><strong>Rejected: PSWriteOffice / community OpenXML wrapper modules.</strong> These exist (PowerShell Gallery) but are pre-1.0 / experimental (<code>PSWriteOffice</code> is at <code>0.0.2</code>). Depending on an unstable third-party wrapper trades one dependency risk for another; referencing <code>DocumentFormat.OpenXml</code> directly is the more stable pure-.NET path and keeps AzureScout&#39;s supply chain to Microsoft-published packages, matching how the Excel tier already depends on EPPlus (a mature, versioned package) rather than a thin wrapper around it.</li></ul><h2 id="_3-what-changes-if-adopted" tabindex="-1">3. What changes if adopted <a class="header-anchor" href="#_3-what-changes-if-adopted" aria-label="Permalink to &quot;3. What changes if adopted&quot;">​</a></h2><ul><li><strong><code>src/report/renderers/Export-Pptx.ps1</code> gets rewritten.</strong> The <code>python &quot;$PSScriptRoot/../templates/build_deck.py&quot; ...</code> shell-out is replaced with PowerShell that loads <code>DocumentFormat.OpenXml</code> via <code>Add-Type</code>, opens <code>deck.pptx.template</code>, and populates it from the scored <code>findings.json</code> object — same function signature (<code>Export-Pptx -Findings $Findings -OutputPath $OutputPath</code>), same <code>Export-Report.ps1</code> dispatch contract (<code>src/report/Export-Report.ps1</code>).</li><li><strong><code>src/report/templates/build_deck.py</code> is removed.</strong> Its slide content (title slide, framework scores, area scores, top-10 gaps, manual-review worklist — including the AB#5089 defensive <code>sev()</code> null-severity guard) becomes the spec for the new PowerShell implementation; the guard logic must be carried over, not dropped.</li><li><strong>A new template asset is added</strong>: <code>src/report/templates/deck.pptx.template</code> (referenced in <code>enhancement-spec.md</code> §1.1 but not present in the current prototype tree) — a PowerPoint file with the title layout and one styled content-slide layout (navy <code>#1F4E78</code> branding, matching <code>report.html.template</code>), authored once in PowerPoint Desktop, committed as a binary asset like <code>report.pbit</code> already is for the Power BI tier.</li><li><strong><code>AzureScout.psm1</code>&#39;s <code>$_requiredModules</code> list gains an entry</strong> for the OpenXML dependency. Two viable forms — pick one at implementation time: install <code>DocumentFormat.OpenXml</code> as a NuGet package and load with <code>Add-Type -Path</code>, or (simpler for a <code>$_requiredModules</code> + <code>Install-Module</code> pattern that mirrors the existing <code>ImportExcel</code>/<code>Az.*</code> entries) depend on a PowerShell Gallery module that itself references the SDK assembly. This repo&#39;s existing bootstrap pattern (<code>AzureScout.psm1:21-42</code>) favors a gallery module if one is stable enough by implementation time; otherwise a <code>NuGet</code>-sourced <code>Add-Type -Path</code> load next to the other dependency bootstrap code.</li><li><strong><code>.ado/azure-pipelines.yml</code> needs no new step.</strong> No <code>pip install</code>, no Python provisioning — the Linux Microsoft-hosted agent already has the .NET runtime PowerShell 7 itself needs; nothing extra to add for this tier.</li><li><strong><code>docs/design/enhancement-spec.md</code> §6.4 and §10 stay as historical record</strong> (the spec is &quot;verbatim source of record,&quot; per its own frontmatter) — they are not edited to reflect this decision. The divergence from the spec&#39;s python-pptx default is captured here instead.</li></ul><h2 id="_4-what-this-does-not-decide" tabindex="-1">4. What this does NOT decide <a class="header-anchor" href="#_4-what-this-does-not-decide" aria-label="Permalink to &quot;4. What this does NOT decide&quot;">​</a></h2><ul><li><strong>Power BI (Tier 1) is unaffected.</strong> <code>Export-PowerBi.ps1</code> continues emitting flat CSVs against a <code>.pbit</code> template; nothing here touches it.</li><li><strong>Self-contained HTML (Tier 2) is unaffected.</strong> <code>Export-Html.ps1</code> and <code>report.html.template</code> are untouched.</li><li><strong>Excel/JSON (evidence tier) are unaffected.</strong></li><li><strong>This is not final.</strong> It is a recommendation for the repo owner to confirm before AB#5044 (tiered renderer engine) is built out against it. If the owner prefers to accept the Python dependency (e.g., because <code>python-pptx</code> is already provisioned elsewhere in their environment), that is a valid override — record it as an amendment to this file rather than a silent reversal.</li></ul><h2 id="_5-implementation-note-ab-5044-2026-07-23" tabindex="-1">5. Implementation note (AB#5044, 2026-07-23) <a class="header-anchor" href="#_5-implementation-note-ab-5044-2026-07-23" aria-label="Permalink to &quot;5. Implementation note (AB#5044, 2026-07-23)&quot;">​</a></h2><p><strong>Status stays Accepted.</strong> <code>src/report/renderers/Export-Pptx.ps1</code> is implemented per §1–§3 above, with two deliberate amendments to the original plan, both because a designer-authored binary <code>.pptx</code> cannot be committed to this repo (§3&#39;s original text assumed one would be):</p><ul><li><strong>Template-as-code, not a committed <code>deck.pptx.template</code> binary.</strong> Instead of opening a pre-authored <code>.pptx</code> and cloning its slide layouts, the deck&#39;s entire visual design — theme (navy <code>#1F4E78</code> / steel <code>#2E75B6</code> / gold <code>#B8860B</code> palette, matching <code>report.html.template</code>), slide master, title layout, and content layout — is built programmatically in <code>New-ScoutDeckShell</code> and the shared <code>Add-ScoutSlideChrome</code> function. This is the &quot;template-driven approach&quot; the acceptance criterion asks for, just authored in PowerShell instead of PowerPoint Desktop. <strong>Extension point:</strong> a future designer-authored <code>deck.pptx.template</code> can still replace this — swap <code>New-ScoutDeckShell</code>&#39;s theme/master/layout construction for code that opens the template and clones its parts (<code>SlideMasterPart</code>/<code>SlideLayoutPart</code>/<code>ThemePart</code>), and leave every slide-content builder (<code>New-ScoutTitleSlide</code>, <code>New-ScoutExecSummarySlide</code>, <code>New-ScoutAreaTableSlides</code>, <code>New-ScoutGapsSlides</code>, <code>New-ScoutManualSlide</code>, <code>New-ScoutNextStepsSlide</code>) untouched — they only depend on the shell exposing a title <code>SlideLayoutPart</code> and a content <code>SlideLayoutPart</code>. This extension point is also called out in a code comment at the top of <code>Export-Pptx.ps1</code>.</li><li><strong>Assembly acquisition instead of a <code>$_requiredModules</code> PowerShell Gallery entry.</strong> No PSGallery wrapper module for <code>DocumentFormat.OpenXml</code> was stable enough to depend on at implementation time (the rejection of <code>PSWriteOffice</code> in §2 still applies), so <code>Export-Pptx.ps1</code> acquires the pinned <code>DocumentFormat.OpenXml</code> NuGet package itself on first use: it shells out to <code>dotnet build</code> against a throwaway class-library project referencing the package, which resolves the full transitive dependency graph (<code>DocumentFormat.OpenXml.Framework</code>, <code>System.IO.Packaging</code>) via NuGet/MSBuild rather than hand-rolling flat-container downloads, and caches the three resulting DLLs under <code>output/.tools/openxml/&lt;version&gt;/</code> (already <code>.gitignore</code>d — no binaries committed). Subsequent calls in the same or later runs load straight from the cache. If offline and nothing is cached, <code>Export-Pptx</code> throws a clear, actionable error rather than silently skipping the deck or faking success — the same failure mode the original python-pptx prototype had for a missing <code>python</code> binary, now fixed instead of inherited.</li><li><strong><code>Export-Pptx</code> gained an optional <code>-Collect</code> parameter</strong>, matching <code>Export-Html</code>/<code>-Excel</code>/<code>-PowerBi</code>&#39;s existing signature (the original §3 text expected the signature to stay <code>-Findings</code>/<code>-OutputPath</code> only). <code>Collect._meta.scope</code> / <code>.managementGroupId</code> are shown on the title slide when present; the deck degrades gracefully to date + branding only when <code>-Collect</code> is omitted or empty, since neither <code>Get-Score</code>&#39;s Findings contract nor <code>src/collect/Invoke-Collect.ps1</code>&#39;s canonical collect shape carries a tenant field today.</li><li>The AB#5089 defensive severity guard (null/unknown severity sorts LAST, never throws) is carried into the renderer as <code>Get-ScoutSeverityRank</code>/<code>Get-ScoutSeverityLabel</code>, applied redundantly at render time even though <code>Get-Score</code> already sorts gaps this way — same belt-and-suspenders intent as the retired <code>build_deck.py</code>&#39;s <code>sev()</code> helper.</li></ul><p>Smoke-tested via <code>tests/Test-PptxFromDataDump.ps1</code> (synthetic findings derived from <code>tests/datadump/sample-report.json</code>, including deliberately null/blank/unrecognized severities): renders an 8-slide deck, validates as a well-formed OPC package, and passes <code>DocumentFormat.OpenXml.Validation.OpenXmlValidator</code> with zero schema issues.</p>',15)])])}const m=t(d,[["render",a]]);export{u as __pageData,m as default};
