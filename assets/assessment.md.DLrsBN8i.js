import{_ as s,o as a,c as t,a2 as n}from"./chunks/framework.il0noOp3.js";const u=JSON.parse('{"title":"CAF/WAF Assessment Platform","description":"Run a read-only CAF/WAF assessment with AzureScout — architecture, every run mode, all 22 assessments, and minimum auth per scan type.","frontmatter":{"description":"Run a read-only CAF/WAF assessment with AzureScout — architecture, every run mode, all 22 assessments, and minimum auth per scan type."},"headers":[],"relativePath":"assessment.md","filePath":"assessment.md"}'),i={name:"assessment.md"};function o(r,e,l,d,c,p){return a(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="caf-waf-assessment-platform" tabindex="-1">CAF/WAF Assessment Platform <a class="header-anchor" href="#caf-waf-assessment-platform" aria-label="Permalink to &quot;CAF/WAF Assessment Platform&quot;">​</a></h1><p>Introduced in <strong>v2.0.0</strong>, AzureScout can score a tenant against the Microsoft <strong>Cloud Adoption Framework (CAF)</strong> design areas and <strong>Well-Architected Framework (WAF)</strong> pillars — not just inventory it. The assessment is <strong>read-only</strong> end to end (ARM Reader at the management-group root, plus read-only Graph app permissions for a subset of scans — see <a href="./assessment-permissions.html">Auth &amp; permissions per scan type</a>).</p><div class="tip custom-block"><p class="custom-block-title">PowerShell 7 required</p><p>The assessment platform requires <strong>PowerShell 7</strong> (every script starts with <code>#Requires -Version 7.0</code>). The v1 inventory features (<code>Invoke-AzureScout</code>) still run on Windows PowerShell 5.1. Full details, including a module bootstrap gap you need to work around manually: <a href="./assessment-prerequisites.html">Assessment Prerequisites</a>.</p></div><h2 id="architecture-—-three-layers-json-on-disk" tabindex="-1">Architecture — three layers, JSON on disk <a class="header-anchor" href="#architecture-—-three-layers-json-on-disk" aria-label="Permalink to &quot;Architecture — three layers, JSON on disk&quot;">​</a></h2><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>COLLECT  --collect.json--&gt;  ASSESS  --findings.json--&gt;  REPORT</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Each layer runs independently from its JSON input, so you can collect once and assess later, or re-render reports from an existing findings set without re-scanning.</p><table tabindex="0"><thead><tr><th>Layer</th><th>What it does</th></tr></thead><tbody><tr><td><strong>Collect</strong></td><td>Read-only Azure Resource Graph queries produce a normalized <code>collect.json</code>, including a per-domain <code>domains.*</code> namespace.</td></tr><tr><td><strong>Ingest</strong></td><td>Folds governance data (natively collected by default — see below), an ARG query pack, and Azure Advisor into the same <code>collect.json</code>.</td></tr><tr><td><strong>Assess</strong></td><td>A declarative rule engine grades the collected data — <strong>139 rules across 8 CAF design areas + 5 WAF pillars</strong> — producing scored <code>findings.json</code> with a prioritized gap list.</td></tr><tr><td><strong>Report</strong></td><td>Renders <code>findings.json</code> into tiered deliverables.</td></tr></tbody></table><div class="info custom-block"><p class="custom-block-title">Governance data is collected natively — no AzGovViz dependency</p><p><code>Import-Governance</code> (<code>src/ingest/Import-Governance.ps1</code>) is the <strong>default</strong> governance collector for the five assessments that need governance data (<code>LandingZone</code>, <code>Management</code>, <code>Identity</code>, <code>Governance</code>, <code>Policy</code> — their manifest <code>Ingest</code> value is <code>Governance</code>, not <code>AzGovViz</code>). It populates <code>collect.json</code>&#39;s <code>governance</code> object natively from Azure Resource Graph (policy assignments, role assignments, management groups) plus ambient-token ARM REST calls (budgets, resource locks) — no cloned repo, no <code>AzAPICall</code> install prompt, fully unattended. It needs only <strong>ARM Reader at the management-group root</strong>, same as every other assessment — no additional Graph permission.</p><p>The third-party Azure Governance Visualizer remains available as an <strong>opt-in</strong><code>Ingest</code> value if you want it specifically, but nothing depends on it by default anymore. Live-verified against the HCS tenant: real policy/role assignments are collected, CAF governance/identity rules score real Pass/Fail, and the ALZ benchmark degrades to an explicit <code>Unknown</code> — not a false 0% — when management-group data isn&#39;t visible to the identity running the scan. Two datasets are intentionally always empty: <code>classicAdministrators</code> (a retired API — the CAF-IDN-03 rule asserts <code>notExists</code>, so empty is compliant) and <code>pimEligibility</code> (needs an Entra ID P2 license plus <code>PrivilegedAccess.Read.AzureResources</code>, which only the opt-in AzGovViz path ever requests).</p></div><div class="info custom-block"><p class="custom-block-title">Collect is now actually scoped by category</p><p><code>Invoke-Collect.ps1</code>&#39;s <code>-Categories</code> parameter (populated from each assessment&#39;s declared <code>Collect</code> list, or your <code>-Category</code> override) <strong>does filter which Resource Graph queries run</strong>. Every query in <code>Invoke-Collect</code> is tagged with the <code>Collect</code> category name(s) whose rule files reference its output — cross-domain references included (e.g. <code>waf.security</code> needs <code>domains.databases.sqlServers</code>, so that query runs for both <code>Databases</code> and <code>Security</code>). <code>subscriptions</code> always runs (base data every rule set needs). Passing <code>-Categories &#39;*&#39;</code> (or an empty list, or omitting <code>-Category</code>/leaving an assessment&#39;s own <code>Collect</code> list at <code>@(&#39;*&#39;)</code> — <code>LandingZone</code> and <code>Estate</code> both do this) runs every query, same as before. The practical effect: <code>-Assessment Security</code> now collects a materially smaller set of resource types than <code>-Assessment LandingZone</code> (see the run below) — this is a different mechanism from <code>Invoke-AzureScout</code>&#39;s module-loading-level <a href="./category-filtering.html">Category Filtering</a>, but for the assessment platform it now actually shrinks scan time and query volume, not just what gets scored.</p><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Pulls only Security-relevant resource types (Key Vaults, NSGs, private</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># endpoints/DNS zones, SQL servers, ...) instead of the full ~25-query set.</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment Security </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat Json</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div></div><h2 id="run-modes" tabindex="-1">Run modes <a class="header-anchor" href="#run-modes" aria-label="Permalink to &quot;Run modes&quot;">​</a></h2><p>Every discovery category is an independently runnable, tagged assessment — pass one, several, or <code>All</code> to <code>-Assessment</code>. All examples assume you&#39;ve installed and imported the module:</p><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Install once, from the PowerShell Gallery (current version 2.0.1)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Install-Module</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Name AzureScout</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Or import from a local clone</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Import-Module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> .</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">AzureScout.psd1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><h3 id="full-landing-zone-assessment" tabindex="-1">Full landing-zone assessment <a class="header-anchor" href="#full-landing-zone-assessment" aria-label="Permalink to &quot;Full landing-zone assessment&quot;">​</a></h3><p>Scores all 8 CAF areas and all 5 WAF pillars in one run, against an ALZ benchmark diff.</p><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat All</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h3 id="single-category" tabindex="-1">Single category <a class="header-anchor" href="#single-category" aria-label="Permalink to &quot;Single category&quot;">​</a></h3><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment Security </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat Html</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h3 id="multiple-assessments-in-one-run" tabindex="-1">Multiple assessments in one run <a class="header-anchor" href="#multiple-assessments-in-one-run" aria-label="Permalink to &quot;Multiple assessments in one run&quot;">​</a></h3><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment Networking</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">,</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Security </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat Html</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Findings from both are combined into one <code>findings.json</code> and one set of reports for the run.</p><h3 id="every-assessment-all" tabindex="-1">Every assessment (<code>All</code>) <a class="header-anchor" href="#every-assessment-all" aria-label="Permalink to &quot;Every assessment (\`All\`)&quot;">​</a></h3><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment All </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat All</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><code>-Assessment All</code> expands to every key in <code>manifests/assessments.psd1</code> — currently <strong>22</strong> assessments (see the <a href="./design/assessment-registry.html">full registry</a>).</p><h3 id="unattended-one-command-run-invoke-scoutpipeline" tabindex="-1">Unattended, one-command run (<code>Invoke-ScoutPipeline</code>) <a class="header-anchor" href="#unattended-one-command-run-invoke-scoutpipeline" aria-label="Permalink to &quot;Unattended, one-command run (\`Invoke-ScoutPipeline\`)&quot;">​</a></h3><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutPipeline</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat All </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputPath </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;D:\\Reports\\Scout&#39;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><code>Invoke-ScoutPipeline</code> (exported public cmdlet, <code>src/Invoke-ScoutPipeline.ps1</code>) runs collect → assess → report <strong>headless</strong> in one call, writing everything into a single dated run folder. It is non-interactive throughout — it forces <code>$ConfirmPreference = &#39;None&#39;</code> and <code>$ProgressPreference = &#39;SilentlyContinue&#39;</code> for the duration of the run. By default it runs the read-only permission pre-flight first (pass <code>-SkipPermissionAudit</code> to skip it), and it wraps the orchestrator in try/catch so a failure in one exporter degrades the run to <code>PartialSuccess</code> rather than losing the output that did succeed.</p><p>It writes two summary files into the run folder alongside the usual report tiers:</p><ul><li><code>pipeline-summary.json</code> — CI-facing: <code>schemaVersion</code>, <code>startedOn</code> / <code>finishedOn</code>, <code>elapsedSeconds</code>, <code>assessments</code>, <code>formats</code>, <code>findingsByStatus</code>, <code>permissionAudit</code>, and <code>outcome</code> (one of <code>Success</code>, <code>PartialSuccess</code>, <code>Failed</code>).</li><li><code>pipeline-summary.md</code> — the human-readable equivalent.</li></ul><p><code>Invoke-ScoutPipeline</code> returns the run-folder path. It only throws (and sets <code>$LASTEXITCODE = 1</code>) when <code>outcome</code> is <code>Failed</code> — a <code>PartialSuccess</code> outcome returns normally so a CI step can inspect <code>pipeline-summary.json</code> and decide what to do with a partial run.</p><p>Parameters: <code>-Assessment</code>, <code>-OutputFormat</code> (default <code>All</code>), <code>-OutputPath</code>, <code>-ManagementGroupId</code>, <code>-Category</code>, <code>-SkipPermissionAudit</code> — the same run-mode semantics as <code>Invoke-ScoutAssessment</code> described throughout this page apply here too.</p><h3 id="collect-once-assess-later-collectonly-fromcollect" tabindex="-1">Collect once, assess later (<code>-CollectOnly</code> / <code>-FromCollect</code>) <a class="header-anchor" href="#collect-once-assess-later-collectonly-fromcollect" aria-label="Permalink to &quot;Collect once, assess later (\`-CollectOnly\` / \`-FromCollect\`)&quot;">​</a></h3><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Stop after Collect — writes collect.json and returns its path</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CollectOnly</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Re-run Assess + Report from that saved collect.json, no re-scan</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FromCollect .</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">output</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">20260720_101500</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">collect.json </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat PowerBi</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Useful for iterating on rule changes or re-rendering a different output tier without re-querying Azure.</p><h3 id="permission-pre-flight-permissionaudit" tabindex="-1">Permission pre-flight (<code>-PermissionAudit</code>) <a class="header-anchor" href="#permission-pre-flight-permissionaudit" aria-label="Permalink to &quot;Permission pre-flight (\`-PermissionAudit\`)&quot;">​</a></h3><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">,</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Identity </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PermissionAudit</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Checks read-only access for the requested assessment(s) <strong>before</strong> any collection runs — see <a href="./assessment-permissions.html">Auth &amp; permissions per scan type</a> for exactly what this does and does not verify.</p><h3 id="scoping-to-a-management-group-managementgroupid" tabindex="-1">Scoping to a management group (<code>-ManagementGroupId</code>) <a class="header-anchor" href="#scoping-to-a-management-group-managementgroupid" aria-label="Permalink to &quot;Scoping to a management group (\`-ManagementGroupId\`)&quot;">​</a></h3><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ManagementGroupId </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;contoso-root-mg&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat Html</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><div class="warning custom-block"><p class="custom-block-title">Scopes Collect too now — and the benchmark still needs MG-root visibility</p><p><code>-ManagementGroupId</code> is passed to <code>Invoke-Collect</code> / <code>Invoke-ArgQueryPack</code>, which pass it through as <code>Search-AzGraph -ManagementGroup</code> on every Resource Graph query (and, if you&#39;ve opted into the legacy <code>AzGovViz</code> ingestor, to <code>Import-AzGovViz</code> too). Omit it and Collect keeps tenant-wide behavior (no <code>-ManagementGroup</code> filter is passed to <code>Search-AzGraph</code> at all — not an empty/wildcard scope, the parameter is left off entirely).</p><p>For the 5 assessments that ingest governance data (<code>LandingZone</code>, <code>Management</code>, <code>Identity</code>, <code>Governance</code>, <code>Policy</code>), the <strong>native</strong><code>Import-Governance</code> collector (the default <code>Ingest = Governance</code>) runs regardless of whether <code>-ManagementGroupId</code> is supplied — it does not silently skip. What actually needs management-group visibility is the <strong>ALZ benchmark diff</strong>: if the identity running the scan doesn&#39;t have Reader at the management-group root (with or without <code>-ManagementGroupId</code> set), the benchmark degrades to an explicit <code>Unknown</code> — not a false 0% — rather than failing loudly. See <a href="./assessment-permissions.html#-managementgroupid-and-governance-data-collection">Auth &amp; permissions per scan type</a>.</p></div><h3 id="scope" tabindex="-1"><code>-Scope</code> <a class="header-anchor" href="#scope" aria-label="Permalink to &quot;\`-Scope\`&quot;">​</a></h3><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Scope All        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># default</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Scope ArmOnly    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># identical to All today</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Scope EntraOnly  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># throws -- see below</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><div class="info custom-block"><p class="custom-block-title">EntraOnly throws instead of silently collecting nothing</p><p>The assessment platform&#39;s Collect layer is ARG/ARM only — there is no Graph-based collection path in <code>Invoke-Collect</code>. <code>-Scope EntraOnly</code> used to be accepted and silently produce a run that could never gather any data; it now <strong>throws immediately</strong> with a redirect to the tool that actually has an Entra collection path:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Invoke-ScoutAssessment collects ARM/Resource Graph data only -- the assessment</span></span>
<span class="line"><span>platform&#39;s Collect layer has no Entra ID collection path. Use</span></span>
<span class="line"><span>&#39;Invoke-AzureScout -Scope EntraOnly&#39; for Entra ID inventory instead.</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>ArmOnly</code> and <code>All</code> remain accepted and behave identically (both just run the ARM collect) — kept for forward compatibility rather than removed, since <code>Invoke-Collect</code> has no ARM-vs-Entra branch to differentiate them. This differs from <code>Invoke-AzureScout -Scope</code>, which does gate ARM vs. Entra extraction in the v1 inventory tool (see <a href="./usage.html#scope">Usage Guide</a>) — use that cmdlet for Entra ID inventory.</p></div><h3 id="category-override" tabindex="-1"><code>-Category</code> override <a class="header-anchor" href="#category-override" aria-label="Permalink to &quot;\`-Category\` override&quot;">​</a></h3><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment Compute </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Category Compute</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">,</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Storage</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><code>-Category</code> replaces the categories recorded for the run — and, per the note above, this now <strong>does</strong> change what <code>Invoke-Collect</code> actually queries (it runs only the queries tagged for the categories you pass, plus base data). It never changes which <strong>rules</strong> are scored (<code>Compute</code>&#39;s <code>Rules</code> stay <code>waf.reliability</code>/<code>waf.cost</code>/<code>waf.performance</code> no matter what <code>-Category</code> you pass) — so overriding <code>-Category</code> to something narrower than what those rules need can starve them of data (they&#39;ll show <code>Unknown</code>/fail-vacuously instead of a real result). Prefer leaving <code>-Category</code> unset and letting each assessment use its own manifest-declared <code>Collect</code> list, which is kept in sync with what its <code>Rules</code> actually reference.</p><h3 id="outputformat-—-one-example-per-tier" tabindex="-1"><code>-OutputFormat</code> — one example per tier <a class="header-anchor" href="#outputformat-—-one-example-per-tier" aria-label="Permalink to &quot;\`-OutputFormat\` — one example per tier&quot;">​</a></h3><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat PowerBi</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat Html</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat Pptx</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat Excel</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat Json</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat React</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputFormat All     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># PowerBi, Html, Pptx, Excel, Json, React</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p><code>-OutputFormat</code> also accepts an array (<code>-OutputFormat Html,Pptx</code>). <code>React</code> produces a single self-contained <code>report-react.html</code> — see <a href="#report-tiers">Report tiers</a> below — and is also available on <code>Invoke-ScoutPipeline</code> via its own <code>-OutputFormat</code> parameter.</p><h3 id="outputpath" tabindex="-1"><code>-OutputPath</code> <a class="header-anchor" href="#outputpath" aria-label="Permalink to &quot;\`-OutputPath\`&quot;">​</a></h3><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">OutputPath </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;D:\\Reports\\Scout&#39;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Each run writes into a timestamped subfolder (<code>&lt;OutputPath&gt;/yyyyMMdd_HHmmss/</code>).</p><h2 id="all-22-assessments" tabindex="-1">All 22 assessments <a class="header-anchor" href="#all-22-assessments" aria-label="Permalink to &quot;All 22 assessments&quot;">​</a></h2><p>The full catalogue — description, <code>Collect</code>/<code>Ingest</code>, CAF areas / WAF pillars, and default report tiers, generated from <code>manifests/assessments.psd1</code> — lives in the <strong><a href="./design/assessment-registry.html">Assessment Registry</a></strong>. Minimum auth per assessment lives in <strong><a href="./assessment-permissions.html">Auth &amp; permissions per scan type</a></strong>.</p><h2 id="scoring" tabindex="-1">Scoring <a class="header-anchor" href="#scoring" aria-label="Permalink to &quot;Scoring&quot;">​</a></h2><ul><li>Each rule evaluates to <strong>Pass</strong>, <strong>Fail</strong>, <strong>Partial</strong>, <strong>Manual</strong>, <strong>Unknown</strong>, or <strong>Error</strong>.</li><li>Framework scores are the rule-count-weighted roll-up of area/pillar scores.</li><li><code>Unknown</code>/<code>Error</code> are surfaced, never silently dropped — a broken rule cannot inflate a score.</li><li>The <strong><code>Manual</code></strong> status intentionally hands the un-automatable checks to a human, with the collected evidence already attached.</li></ul><h2 id="cross-run-drift" tabindex="-1">Cross-run drift <a class="header-anchor" href="#cross-run-drift" aria-label="Permalink to &quot;Cross-run drift&quot;">​</a></h2><p><code>Get-ScoutDrift</code> computes drift between the current run and the previous run for the same assessment: each finding is classified <strong>New</strong>, <strong>Resolved</strong> (<code>Fail</code>/<code>Partial</code> → <code>Pass</code>), <strong>Regressed</strong> (<code>Pass</code> → <code>Fail</code>/<code>Partial</code>), or <strong>Unchanged</strong>, plus an overall weighted score delta. History is kept in an append-only <code>findings-history.json</code> under a <code>.scout-history/</code> folder in the output root, keyed by run id — the first run for a given assessment becomes the baseline (nothing to diff against yet). <code>Invoke-ScoutAssessment</code> computes drift automatically after scoring and feeds it into the <a href="#report-tiers">React report</a>&#39;s Drift tab; a drift computation failure is non-fatal to the rest of the run.</p><h2 id="report-tiers" tabindex="-1">Report tiers <a class="header-anchor" href="#report-tiers" aria-label="Permalink to &quot;Report tiers&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Tier</th><th>Output</th><th>Notes</th></tr></thead><tbody><tr><td>Power BI</td><td><code>powerbi/*.csv</code> + <code>.pbit</code></td><td>Primary analytics tier (star schema)</td></tr><tr><td>HTML</td><td><code>report.html</code></td><td>Self-contained, single file</td></tr><tr><td>PowerPoint</td><td><code>assessment_deck.pptx</code></td><td>Executive deck via the OpenXML SDK — <strong>no Python dependency</strong>. First use needs the <code>dotnet</code> SDK; see <a href="./assessment-prerequisites.html#powerpoint-tier-net-sdk-not-python">Assessment Prerequisites</a>.</td></tr><tr><td>Excel</td><td><code>assessment_evidence.xlsx</code></td><td>Evidence tier</td></tr><tr><td>JSON</td><td><code>findings.json</code></td><td>The machine-readable contract</td></tr><tr><td>React</td><td><code>report-react.html</code></td><td>Self-contained (CSS/JS inline, findings embedded as a JSON blob, no external/CDN requests). Client-side filter by Framework/Area/Severity/Status, sortable/searchable findings table, a summary dashboard, and a Drift tab showing cross-run drift (see <a href="#cross-run-drift">Cross-run drift</a>).</td></tr></tbody></table><h2 id="minimum-auth-per-scan-type" tabindex="-1">Minimum auth per scan type <a class="header-anchor" href="#minimum-auth-per-scan-type" aria-label="Permalink to &quot;Minimum auth per scan type&quot;">​</a></h2><ul><li><strong>Every assessment</strong> needs <strong>ARM <code>Reader</code> at the tenant-root management group</strong>. No exceptions.</li><li>The 5 assessments that ingest governance data (<code>LandingZone</code>, <code>Management</code>, <code>Identity</code>, <code>Governance</code>, <code>Policy</code>) use the <strong>native</strong> <code>Import-Governance</code> collector by default — ARM Reader at the MG root is enough for them too; no Graph permission is required by default. The ALZ benchmark specifically needs that MG-root visibility to fully resolve; without it, it degrades to an explicit <code>Unknown</code> rather than a false 0%.</li><li>Microsoft Graph <strong>application</strong> permissions are only needed if you opt one of those 5 assessments into the legacy <code>AzGovViz</code> ingestor instead of the native default — see <a href="./assessment-permissions.html">Auth &amp; permissions per scan type</a> for exactly which permissions and when.</li><li><code>PrivilegedAccess.Read.AzureResources</code> needs an <strong>Entra ID P2 license</strong> and, even on the opt-in <code>AzGovViz</code> path, is currently never exercised — <code>Import-AzGovViz.ps1</code> unconditionally passes <code>-NoPIMEligibility</code>. The native <code>Import-Governance</code> collector doesn&#39;t collect PIM-eligible role assignments either (<code>pimEligibility</code> is intentionally always empty for the same license/permission reason).</li></ul><p>Full matrix, the exact permissions list, and what <code>-PermissionAudit</code> does and does not verify: <strong><a href="./assessment-permissions.html">Auth &amp; permissions per scan type</a></strong>.</p><div class="language-powershell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Pre-flight before any collection runs</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Invoke-ScoutAssessment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Assessment LandingZone </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PermissionAudit</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><h2 id="design-reference" tabindex="-1">Design reference <a class="header-anchor" href="#design-reference" aria-label="Permalink to &quot;Design reference&quot;">​</a></h2><p>The full architecture, rule catalogue, and decision records live in the <a href="./design/master-plan.html">Master Design &amp; Plan</a> and the <a href="./design/assessment-registry.html">assessment registry</a>.</p>`,65)])])}const k=s(i,[["render",o]]);export{u as __pageData,k as default};
